class Card {
    constructor(id, word, translate, img, audio) {
        this.id = id;
        this.word = word;
        this.translate = translate;
        this.img = img;
        this.audio = audio;
    }
}

export default Card;